import FormNoControlado from './FormNoControlado';
import Formulario from './Formulario';

function Main() {
    return (
        <main>
            <h1>Formularios</h1>
            <Formulario />
            <FormNoControlado />
        </main>
    );
}

export default Main;
